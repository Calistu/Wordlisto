int criar ();

extern char insp(char ascii,int opc);

extern int custommer();

#define CASE 1
#define BINAR "wb"
#define TXT "w"

#define RD "r"
#define AP "a+"
#define WR "w" // replace with "a+" if want to increment wordlists with the same name.

extern char *list[17];

extern char  caminho[20];
